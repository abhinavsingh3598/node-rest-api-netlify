{
  "name": "restapi",
  "version": "1.0.0",
  "description": "creating a rest api using node.js",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build":"NODE_ENV=production ./node_modules/.bin/netlify-lambda build src"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "netlify-cli": "^13.1.2",
    "netlify-lambda": "^2.0.16",
    "serverless-http": "^3.2.0",
    "uuid": "^9.0.0"
  }
}
